{
  "name": "automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run_spec": "npx cypress run --spec cypress/integration/webdriver-uni/mouse-action.js",
    "cy:run_allbrowsers": "npm run cy:run_spec -- --browser chrome & npm run cy:run_spec -- --browser electron & npm run cy:run_spec -- --browser edge & npm run cy:run_spec -- --browser firefox",
    "triggerAllTest-headed": "npx cypress run --headed --browser edge",
    "triggerAllTest-webdriveruni": "npx cypress run --spec cypress/integration/webdriver-uni/* --headless",
    "triggerAllTest-autoTestStore": "npx cypress run --spec cypress/integration/automation-test-store/* --headless",
    "triggerAllTest-autoTestStore-dashboard": "npx cypress run --spec cypress/integration/automation-test-store/* --record --key 1cd01925-701c-42ec-8fad-ecab986ddd34",
    "junit-merge": "npx junit-merge -d cypress/results/junit -o cypress/results/junit/results.xml",
    "delete-junit-report": "rm -rf cypress/results/junit/results.xml",
    "delete-all-reports": "rm -rf cypress/results/* || true",
    "mochawesome-merge": "npx mochawesome-merge cypress/results/mochawesome/*.json > mochawesome.json && npx marge mochawesome.json -i true",
    "delete-mochawesome-report": "rm -rf mochawesome-report/* || true",
    "cypress-regression-pack": "npm run delete-all-reports && npm run delete-mochawesome-report && npx cypress run && npm run mochawesome-merge",
    "cypress-multi-browser": "npm run triggerAllTest-autoTestStore -- --browser electron --headed & npm run triggerAllTest-autoTestStore -- --browser edge --headed ",
    "triggerAllTests-cucumberTests": "npx cypress run --spec cypress/integration/features/*.feature -b edge"
  },
  "author": "bgd",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.7",
    "cypress-file-upload": "^5.0.8",
    "cypress-real-events": "^1.7.0",
    "npx": "^10.2.2"
  },
  "devDependencies": {
    "cypress": "^10.0.2",
    "cypress-cucumber-preprocessor": "^4.3.1",
    "cypress-multi-reporters": "^1.6.0",
    "cypress-xpath": "^1.6.2",
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.0.2",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.1",
    "mochawesome-report-generator": "^6.2.0"
  }
}
